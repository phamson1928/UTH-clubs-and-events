const fs = require('fs'); const path = require('path'); function walk(dir, cb) { fs.readdirSync(dir).forEach(f => { let p = path.join(dir, f); if (fs.statSync(p).isDirectory()) walk(p, cb); else cb(p); }); } walk('d:/UTH-clubs-and-events/uth-club-frontend/src', f => { if (!f.endsWith('.tsx')) return; let c = fs.readFileSync(f, 'utf8'); let init = c; c = c.replace(/label:\s*[`'"]Dashboard[`'"]/g, 'label: \"Bảng điều khiển\"'); c = c.replace(/label:\s*[`'"]Clubs[`'"]/g, 'label: \"Câu lạc bộ\"'); c = c.replace(/label:\s*[`'"]Events[`'"]/g, 'label: \"Sự kiện\"'); c = c.replace(/label:\s*[`'"]Users[`'"]/g, 'label: \"Người dùng\"'); c = c.replace(/label:\s*[`'"]Requests[`'"]/g, 'label: \"Yêu cầu\"'); c = c.replace(/label:\s*[`'"]Members[`'"]/g, 'label: \"Thành viên\"'); c = c.replace(/label:\s*[`'"]Applications[`'"]/g, 'label: \"Đề án\"'); if (c !== init) { fs.writeFileSync(f, c); console.log('Updated', f); } });
