services:
  backend:
    build: .
    container_name: uth_club_backend
    restart: always
    ports:
      # Nginx on the host is the only public entry point.
      - '127.0.0.1:3001:3000'
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
